using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SequenceElement : MonoBehaviour//, IInteractiveObject
{
    /*public SequenceController ElementOwner;

    public void Highlight(Color color)
    {
        
    }

    public void Press()
    {
        if (ElementOwner.OnSequenceElementInput(this)) 
        {
            Highlight(Color.green);
        }
        else
        {
            Highlight(Color.red);
        }
    }
    
    public void InteractWithObject(InteractionType interactionType, HitInfo hitInfo)
    {
        if (interactionType == InteractionType.Pressed)
        {
            Press();
        }
    }

    public bool IsEnabled()
    {
        return enabled && ElementOwner.CurrentState == SequenceState.Input;
    }

    public bool ValidateNormalOffset(float normalOffsetCos)
    {
        return normalOffsetCos > 0.3f;
    }*/
}
